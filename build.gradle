subprojects {
  ext.versions = [
      lombok: '1.18.0'
  ]

  apply plugin: 'java'
  apply plugin: 'idea'

  group = 'org.renke'
  version = '0.0.1-SNAPSHOT'
  sourceCompatibility = 10

  repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
  }
}
